<!--
Citation for the following code:
Date: 09/05/2024
Authors: Rami Albaroudi and Mohamed Saud, Group 13
Original work with some Jinja expressions adapted from https://github.com/osu-cs340-ecampus/flask-starter-app?tab=readme-ov-file#dynamically-displaying-data-in-a-template
-->

{% extends "template.j2" %}

{% block title %}Tracked Days{% endblock %}

{% block content %}
<h3>Tracked Days Listing</h3>

<table>
    <thead>
        <tr>
            <th>Tracked Day ID</th>
            <th>Date</th>
            <th>Client Name</th>
            <th>Total Calories</th>
            <th>Calorie Target</th>
            <th>Calorie Balance</th>
            <th>Note</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        {% for trackedday in trackeddays %}
        <tr id="trackedday-{{ trackedday.trackedDayID }}">
            <td>{{ trackedday.trackedDayID }}</td>
            <td>{{ trackedday.trackedDayDate }}</td>
            <td>{{ trackedday.clientName }}</td>
            <td>{{ trackedday.trackedDayTotalCalories }}</td>
            <td>{{ trackedday.trackedDayCalorieTarget }}</td>
            <td>{{ trackedday.trackedDayTotalCalories - trackedday.trackedDayCalorieTarget }}</td>
            <td>{{ trackedday.trackedDayNote }}</td>
            <td>
                <button onclick="editTrackedDay({{ trackedday.trackedDayID }})">Edit</button>
            </td>
            <td>
                <button onclick="confirmDelete({{ trackedday.trackedDayID }})">Delete</button>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>


<h3>Add Tracked Day</h3>
<form method="POST" action="/add_tracked_day">
    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Client Name</th>
                <th>Calorie Target</th>
                <th>Note</th>
                <th>Add</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><input type="date" id="trackedDayDate" name="trackedDayDate" required></td>
                <td>
                    <input type="text" id="clientName" name="clientName" list="clientNames" autocomplete="on" required>
                    <datalist id="clientNames">
                        {% for client in clients %}
                        <option data-client-id="{{ client.clientID }}"
                            data-calorie-target="{{ client.clientCalorieTarget }}">
                            {{ client.clientName }}
                        </option>
                        {% endfor %}
                    </datalist>
                </td>
                <td><input type="number" id="trackedDayCalorieTarget" name="trackedDayCalorieTarget"></td>
                <td><input type="text" id="trackedDayNote" name="trackedDayNote"></td>
                <td><button type="submit">Add</button></td>
            </tr>
        </tbody>
    </table>
</form>

<script>
    function editTrackedDay(trackedDayID) {
        let row = document.getElementById('trackedday-' + trackedDayID);
        let cells = row.getElementsByTagName('td');

        let date = cells[1].innerText;
        let clientName = cells[2].innerText;
        let calorieTarget = cells[4].innerText;
        let note = cells[6].innerText;

        cells[1].innerHTML = '<input type="date" value="' + date + '">';
        cells[2].innerHTML = '<input type="text" value="' + clientName + '">';
        cells[4].innerHTML = '<input type="number" value="' + calorieTarget + '">';
        cells[6].innerHTML = '<input type="text" value="' + note + '">';
        cells[7].innerHTML = '<button onclick="updateTrackedDay(' + trackedDayID + ')">Update</button>';
    }

    function updateTrackedDay(trackedDayID) {
        let row = document.getElementById('trackedday-' + trackedDayID);
        let cells = row.getElementsByTagName('td');

        // Get the updated values from the input fields
        let date = cells[1].getElementsByTagName('input')[0].value;
        let clientName = cells[2].getElementsByTagName('input')[0].value;
        let calorieTarget = cells[4].getElementsByTagName('input')[0].value;
        let note = cells[6].getElementsByTagName('input')[0].value;

        // Update the table cells with the new values
        cells[1].innerHTML = date;
        cells[2].innerHTML = clientName;
        cells[4].innerHTML = calorieTarget;
        cells[6].innerHTML = note;
        cells[7].innerHTML = '<button onclick="editTrackedDay(' + trackedDayID + ')">Edit</button>';

        // TODO: Implement the logic to send the updated data to the server and update the database
    }

    function confirmDelete(trackedDayID) {
        if (confirm('Are you sure you want to delete this record?')) {
            // TODO: Implement the logic to delete the record from the database
        }
    }
</script>

{% endblock %}